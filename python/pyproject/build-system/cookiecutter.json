{
    "path": "path",
    "builder": ["setuptools", "hatch"],
    "_builders": {
        "setuptools": {
            "requires": ["setuptools>=61.0"],
            "backend": "setuptools.build_meta"
        },
        "hatch": {
            "requires": ["hatchling"],
            "backend": "hatchling.build"
        }
    }
}
